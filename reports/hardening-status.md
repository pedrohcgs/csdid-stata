# Hardening Status

Status: conformance-profile hardening evidence recorded; full-JEL R `did`
2.5.1 oracle parity is closed locally. Final public `v2.0.0` still requires
external platform rows, independent signoffs, release-owner approval, and final
evidence inventory.

Date: 2026-07-09.

This report records the hardening state after the contract fixtures were
promoted from artifact-only divergence evidence to conformance-profile
evidence. The latest full JEL run completes both master scripts, returns
`pass`, and has `oracle_parity_status=pass`. Historical R artifacts drift under
the oracle repin and are recorded separately as release-owner evidence context.

## Verification Completed

- `python3 tools/validate-contract.py` passes.
- All `tests/meta/*.sh` checks pass.
- `tests/run-smoke.sh` passes.
- `tests/run-jel-smoke.sh` passes.
- `CSDID_RUN_JEL_FULL=1 tests/run-jel-full-reproduction.sh` completed on
  2026-07-09, with R and Stata master exit codes `0` and failure markers `0`.
  Figures 3, 8, and 9 match regenerated R `did` 2.5.1 displayed
  estimate/SE/CI labels, and the Table 7 display audit has zero failures.
- Strict Stata log scanning across generated logs found no uncaught failure
  markers in the verified gates.

## Release Hardening Evidence

- Build artifacts are generated by `src/build.do`.
- Public help files were rewritten as user-facing Stata documentation on
  2026-07-07 and are guarded by `tests/meta/test-public-help-surface.sh`.
- Public examples were added under `examples/` and are exercised by
  `tests/stata/test-release-hardening.do`.
- `tests/stata/test-release-hardening.do` covers Mata cache replacement,
  postestimation after failed calls, shuffled-data equivalence, `mata clear`
  recovery, and adversarial covariate/weight/cluster surfaces.
- Isolated install coverage is promoted to the F050 release fixture through
  `tools/parity/generators/f050/generate.R` and `tests/stata/test-f050.do`.
- Release-facing omitted defaults, postestimation handoffs, plot-data export
  diagnostics, and default metadata are promoted to the F051 release fixture
  through `tools/parity/generators/f051/generate.R` and
  `tests/stata/test-f051.do`.
- Direct Stata help files are installed for `csdid`, `csdid_estat`,
  `csdid_stats`, `csdid_plot`, and the postestimation overview; F050 and meta
  tests enforce the package/build surface.
- Default tests do not require network access.
- Build artifacts and Stata logs are ignored by `.gitignore`.
- F049 records the default benchmark budgets for small, medium-panel, and
  medium aggregation workloads, including a binding `<= 1.8x R` literal
  omitted-default row.
- The optional bootstrap plugin is exact against the Mata fallback, selected
  only for eligible seeded Rademacher ATT(g,t) bootstrap, and fails closed.
- The process-level RSS gate covers default cband, seeded and unbalanced
  plugin paths, aggregation bootstrap, and a 500,000-row panel.
- The pinned seven-trial legacy-to-candidate gate covers 15 analytical,
  bootstrap, aggregation, clustering, unbalanced, and large-panel workloads;
  every time and RSS confidence-bound gate passes on the recorded platform.
- JEL smoke, artifact inventory, full reproduction evidence, and semantic
  figure audits cover F041-F044 and JEL001-JEL018.
- ENG001-ENG005 cover the engineering-reference inventory, ado/Mata
  architecture, dependency policy, performance plan, and engineering audit.
- F045 and F046 cover legacy compatibility classification and migration
  guidance.

## Release Materials

- JEL full reproduction result: `reports/jel-full-reproduction-result.md`.
- JEL summary: `reports/jel-replication-summary.md`.
- Engineering audit: `reports/engineering-audit.md`.
- Legacy migration guide: `docs/legacy-migration-guide.md`.
- Release checklist: `docs/release-checklist.md`.
- Release notes seed: `NEWS.md`.

This is not a public Git tag by itself. A release manager must rerun
`docs/release-checklist.md` on the target release machine before publishing,
refresh platform rows, collect independent signoffs, and record final
release-owner approval.
